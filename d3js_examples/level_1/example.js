
d3.select("body")  // выбираем "тело" документа с помощью css-селектора div
    .append("p")  // добавляем к нему новый параграф (тэг <p>)
    .text("New paragraph!");  // устанавливаем текст параграфа
